{
  "name": "airport-weather",
  "version": "1.0.0",
  "private": true,
  "description": "Airport Weather — погода и METAR по ICAO-коду",
  "scripts": {
    "build": "echo 'Static web app — no build step needed'",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}
